# Dashboard container
streamlit==1.39.0
pandas==2.2.3
google-cloud-bigquery==3.26.0
db-dtypes==1.3.0

# Also used when running the ingestion scripts by hand outside Docker
requests==2.32.3
python-dotenv==1.0.1

# Only needed to regenerate the region seed, not at runtime
country_converter==1.2
pycountry==24.6.1
